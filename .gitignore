# La Caja SI esta commiteada en la raiz (facturas/, el Excel, alberto_erp.py),
# por consistencia con main y para que el repo sea autosuficiente. Lo que se
# ignora es un clon local aparte, por si alguien prefiere trabajar asi.
caja/
500-sombras-de-alberto/

venv/
.venv/
__pycache__/
*.pyc
.pytest_cache/
.DS_Store

# Estado y salidas: se regeneran con `alberto todo`
alberto.db
alberto.db-wal
alberto.db-shm
outcomes*.jsonl

# El ejemplo que nos pasaron, conservado como referencia
legacy/

# Paginas rasterizadas de la fase 2 (capa raw). Se regeneran.
raw/
.env

nueva.db

# Frontend
frontend/node_modules/
frontend/.next/
